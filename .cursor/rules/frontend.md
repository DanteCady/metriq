# Metriq Frontend Rules

- Use shared components from packages/ui first
- Do not place business logic in components
- Zustand is only for UI state and mock role switching
- Use tRPC for server data
- Keep pages thin; move reusable UI into components
- Every screen should have loading, empty, and error states