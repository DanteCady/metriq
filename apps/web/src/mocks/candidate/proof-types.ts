export type MockProofHighlight = {
  id: string;
  title: string;
  capability: string;
  artifactId: string;
  /** Optional longer text shown in employer preview when present. */
  summary?: string;
};
